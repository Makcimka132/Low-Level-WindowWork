# Low-Level-WindowWork
Greetings!
LLWW - A library for the C programming language that allows you to work with the higher-level WinAPI.

Example:

#include "llww.h"

int main()
{
    HideConsole();
    msgbox("Test name", "test str");
    return 0;
}

